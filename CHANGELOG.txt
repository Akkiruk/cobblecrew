## 4.4.1 — Logger Tree Fix
- Fix logger harvesting multiple trees at once: remove leaf-peek BFS that bridged adjacent tree canopies
- Tree harvest now uses simple face-connected log BFS (vanilla/Cobblemon trees are always face-connected)

## 4.4.0 — Job Reassignment Overhaul
- Remove 35 underperforming/exploitable jobs (all logistics, most combos, most processing, scout, etc.)
- Reassign qualifying moves across all 50 surviving jobs for better thematic fit
- Add per-job partyEnabled config flag — 32 jobs enabled for party mode, rest pasture-only
- Config schema v3: existing configs auto-migrate to pick up new moves and party defaults
- Rename terrain_flattener → lawnmower
- Move full_restore from combo to support category (now single-move: lifedew)
- Logger now peeks through up to 2 leaf blocks to find hidden logs behind canopy
- Bamboo chopper targets second-to-lowest block — breaking it drops all bamboo above
- Decomposer flood-fills connected leaves (up to 64), each → 1 bone meal
- Stone breaker now mines deepslate and cobbled deepslate
- Excavator now mines grass blocks
- Ice miner now mines blue ice
- Tree planter spacing reduced from 4 to 3 blocks
- Speed booster and haste provider upgraded to level II
- Crop irrigator area reduced to 3×3
- Fishing looter cooldown reduced from 120s to 45s
- Crop sower now supports modded seeds (any item whose block is CropBlock or StemBlock)

## 4.3.2 — Pickup Fix
- Fix pickup never actually running: the idle-phase gate blocked it on every tick
- Pickup now checks during job cooldowns and between assignments (20-tick grace for new jobs)
- In-progress pickups are always continued even across tick boundaries
- Reduce pickup scan cooldown from 3 seconds to 0.5 seconds
- Fix pickup claim guard that prevented continuation when a job was assigned

## 4.3.1 — Pickup Priority
- Ground item pickup now takes priority over job selection
- Pokémon between jobs will always pick up nearby items before starting a new job
- Active jobs are not interrupted — pickup only preempts at the job-selection stage

## 4.3.0 — Randomized Target Selection
- Pokémon now pick a random valid target instead of always the closest one
- Applies to all gathering, environmental, and cauldron-filling jobs
- Workers spread out across available targets instead of crowding the nearest one
- Defense/support/deposit still use closest (closest threat, nearest player, nearest container)
- Slight performance improvement: random selection is O(n) vs O(n log n) for sorted

## 4.2.2 — Honey Harvester Drop Variety
- Honey harvester now has a 50/50 chance to drop 3 honeycomb or 3 honey bottles (previously always honeycomb)

## 4.2.1 — Gathering Job Availability Fix
- Fix gathering jobs with readyChecks (e.g. honey_harvester) falsely claiming availability when no targets are actually ready
- This caused honey_harvester to permanently block bee_pollinator from running, preventing Combee/Vespiquen from filling beehives
- GatheringJob.isAvailable() now checks readyCheck against cached targets before reporting availability

## 4.2.0 — Responsive Workers
- Per-job arrival delay: production, support, environmental jobs now start working instantly instead of waiting 1.5 seconds
- Processing and placement jobs use a short 0.5s delay; gathering and combat keep the full animation
- Live block-change detection: workers now react to broken/placed blocks within 1-2 ticks instead of waiting up to 30 seconds for a full area rescan
- Full area rescans still run as a safety net (default cooldown increased from 8s to 60s)

## 4.1.1 — Stability & Cleanup
- Fix memory leak: server shutdown now clears all singleton state (StateManager, ClaimManager, CacheManager, DeferredBlockScanner, WorkSpeedBoostManager, PartyWorkerManager)
- Fix party worker cleanup ordering bug that could orphan state when player context was missing
- Fix BATTLE_STARTED_PRE error handler using debug level instead of error
- Debug logger now uses LOGGER.debug instead of LOGGER.info to avoid flooding server logs
- Remove 129 no-op cleanup() calls per Pokémon recall (BaseJob.cleanup is handled by StateManager)
- Remove deprecated mob targeting stubs from CobbleCrewNavigationUtils

## 4.1.0 — Party Job Controls
- Per-player party job blocking: /cobblecrew party block job <name> / category <name>
- Per-player party job allowing: /cobblecrew party allow job <name> / category <name>
- /cobblecrew party jobs — view your blocked jobs/categories and server-wide blocks
- /cobblecrew party reset — clear all personal blocks
- Server-wide party blocklists in config (blockedJobs / blockedCategories in party group)
- Party opt-out now persisted to disk (survives server restarts)
- All preferences saved to config/cobblecrew/party-preferences.json
- Blocking a job immediately resets active party workers so changes take effect instantly

## 4.0.0 — V4 Architecture Refactor
- Complete internal rewrite: unified BaseJob state machine replaces 7 separate base classes
- PokemonWorkerState: all per-Pokémon state in one data class (no more scattered UUID maps)
- StateManager: central state registry with profile caching
- ClaimManager: unified target claiming, navigation, and blacklisting
- DepositHelper: shared container-deposit logic for all jobs
- 7 DSL job classes (GatheringJob, ProductionJob, etc.) now thin constructors on BaseJob
- All hand-coded singletons migrated to BaseJob lifecycle
- Config auto-registration: BaseJob.registerConfig() replaces per-job init blocks
- PastureWorkerManager: extracted pasture tick/recall logic from mixin (thin Java shim)
- CobbleCrewCacheManager: flattened to BlockPos keys, removed sealed CacheKey
- HostileScanCache: chunk-granularity cached hostile scans for defense jobs
- IdleBehaviorHandler: extracted idle logic (wander, pickup, return) from WorkerDispatcher
- WorkerDispatcher slimmed from 384 → 235 lines
- Net reduction: ~600+ lines removed across the codebase

## 3.0.49
- Removed all fallbackType/type-based eligibility code — Pokémon type never affects job eligibility
- Only moves and species determine what jobs a Pokémon can do
- Stripped fallbackType from JobConfig, all DSL job classes, API, and commands
- GROWABLE scanner still excludes upper halves of two-block crops (kept from 3.0.48)

## 3.0.48
- (Reverted in 3.0.49) — fallbackType eligibility was added and immediately removed

## 3.0.47
- Fixed hearty grains spinning: scanner no longer caches upper half of two-block crops as separate targets
- isMatureCrop now only returns true for the lower half of hearty grains
- harvestCropDsl always resolves to the lower half for correct loot and cleanup

## 3.0.46
- Fixed stale config override: server config files now auto-migrate when code defaults change (schemaVersion tracking)
- Fixed growth_accelerator spinning: Pokémon now stay on the same crop until it's mature instead of release/re-find cycling
- Growth Accelerator validates target is still an immature crop each tick (releases mature/transformed blocks immediately)

## 3.0.45
- Fixed critical self-sabotage bug: Pokémon's own claim no longer blocks isAvailable(), restoring full harvester throughput
- Added dynamic priority matching: berry harvesters with Pluck now correctly land in MOVE tier instead of TYPE
- Growth Accelerator no longer mistakes mature BerryBlocks as targets (isMatureCrop now handles BerryBlock)
- Doubled pathfinding budget again (6→12 per tick) to support larger worker groups
- Increased claim timeout (5s→10s) to prevent premature claim expiry during work delays
- Added 3-second idle pickup grace period after job drops to prevent immediate item snatching
- Harvesters and environmental workers skip navigateTo when already at target (avoids wasted pathfind calls)

## 3.0.44
- Fixed major throughput bug: work delay was resetting due to navigateTo() fighting handleArrival()
- Pokémon now stop moving immediately upon reaching targets instead of jittering and resetting the 1.5s delay
- Affects ALL job types (harvesting, defending, environmental, placement, support, logistics)
- Doubled pathfinding budget (3→6 per tick) so more Pokémon can acquire targets each dispatch

## 3.0.43
- Fixed major throughput regression: Pokémon were trapped in stickiness loops doing nothing
- isAvailable() now checks whether targets are already claimed by other Pokémon
- Harvesters, defenders, and environmental workers properly go idle when all targets are taken
- Idle pickup claims are released when transitioning to a real job (prevents ghost targets)

## 3.0.42
- Growth accelerator now skips mature crops — no longer wastes time on fully-grown plants
- Fixed hearty grains harvest: properly removes upper half and resets lower half to age 0
- Fixed hearty grains dupe bug when targeting the lower half of a two-block-tall crop

## 3.0.41
- Added job importance system — jobs are now prioritized by urgency within each eligibility tier
- Defense and healing jobs are CRITICAL (handled first when hostiles/damage present)
- Harvesting and gathering jobs are HIGH priority (time-sensitive resources)
- Processing, production, and combat buffs are STANDARD priority
- Placement, growth, and environmental jobs are LOW priority (maintenance)
- Logistics cleanup (magnetizer) is BACKGROUND priority
- Fire dousing is CRITICAL, furnace/brewing fueling is STANDARD (enables processing chain)
- Ground item collector is HIGH (items despawn)
- Utility support buffs (jump, night vision, water breathing) are LOW priority
- Same-importance jobs still shuffle for fairness within each tier

## 3.0.40
- Fixed Pokémon constantly re-pathing to pasture every 5 ticks — now only re-paths every 2 seconds with returning state tracking
- Increased arrival radius from 3 to 5 blocks so Pokémon reliably count as "home"
- Idle Pokémon now wander randomly within 4 blocks of the pasture instead of standing still forever
- Fixed tryIdlePickup causing stuck loops: added 3s scan cooldown, 5s stale claim timeout, and proper item-exists validation
- Pickup scans now filter out removed items and only trigger new scans on cooldown

## 3.0.39
- Fixed Pokémon idling excessively at pastures by overhauling competing delay systems
- Reduced scan cooldown from 20s to 8s — regrown targets are found much faster
- Successful harvests no longer blacklist the position (cache removal is sufficient)
- Job stickiness now breaks early when the sticky job has no available targets
- Fixed escalating blacklist being dead code (queue cleanup was evicting fail counts prematurely)
- Reduced target grace period from 10s to 3s and short blacklist from 10s to 5s
- Pokémon cleanup on recall no longer blacklists abandoned positions

## 3.0.38
- Swapped berry job moves: pluck is now the main berry_harvester move, naturalgift moved to glowberry_picker

## 3.0.37
- Fixed memory leak: recalling Pokémon from pastures now properly cleans up all worker state (jobs, navigation claims, inventory tracking, visual effects)
- Fixed memory leak: breaking a pasture block now cleans up all tethered Pokémon state before removing the cache
- Fixed job switching not cleaning up the outgoing job's internal state (held items, phases, scan caches)
- Fixed config reload (invalidateProfiles) not resetting active job assignments, causing orphaned worker state
- Fixed mob claims using infinite timeout — now uses 30s finite timeout to prevent permanent leaks if a defender is recalled mid-combat
- Added InventoryUtils cleanup to the central cleanupPokemon path (depositArrival, retryTimers, depositWarning maps)
- Added double-init guard to WorkerRegistry

## 3.0.36
- Reworked 39 move-job pairings for better thematic fit
- Every non-combo job now has 70+ eligible species (was as low as 0-5 for some jobs)
- Zero moves are shared across jobs — every move is exclusive to one job
- Notable changes: crop_harvester (harvest→trailblaze), charcoal_burner (burnup→incinerate), bone_grinder (bonemerang→superpower), tree_planter (ingrain→seedbomb), healer (healpulse→drainingkiss), strength_booster (howl→swordsdance)

## 3.0.35
- Added /cobblecrew moves command — analyzes all species learnsets and uploads move report to mclo.gs
- Shows every learnable move, how many species can learn it, and which CobbleCrew job uses it

## 3.0.34
- Every job now requires exactly one unique move — no move is shared across multiple jobs
- Combo jobs each require exactly two unique moves (both must be known)
- Simplified Silk Touch Extractor to a standard combo (Mind Reader + Iron Head)
- Berry Harvester now uses Natural Gift, Tumblestone Harvester now uses Smart Strike
- Updated all combo job move pairs to be unique (see API descriptions for details)

## 3.0.33
- Fixed Tree Feller leaving floating leaves on large trees — leaf cap raised from 256 to 1024
- Leaf seeding now checks diagonal neighbors (all 26 directions) since vanilla trees place leaves diagonally from logs

## 3.0.32
- Reworked tree chopping: merged 3 separate logging jobs (overworld_logger, jungle_chopper, fungi_harvester) into one universal "logger" job
- Logger now fells entire trees in one pass instead of one block at a time — dramatically faster
- Logger uses tag-based detection (minecraft:logs) so it works on all log types including modded
- Any of Cut, Fury Cutter, X-Scissor, or Shadow Claw qualifies a Pokémon for logging
- Tree Feller combo (Cut + Headbutt) now also strips leaves for bonus loot (saplings, sticks, apples)
- Tree Feller now works on all log types, not just overworld logs
- Removed broken top-down one-block-at-a-time tree harvesting
- All broken log positions are purged from cache immediately, preventing other Pokémon from targeting the same tree

## 3.0.31
- Fixed ScoutWorker crashing from off-thread world access — structure locating now runs on the server thread
- Fixed navigateToPlayer() pathfinding every single tick — now throttled to every 10 ticks
- Fixed unreachable block cache growing without bound — expired entries are now swept periodically
- Fixed potential item duplication in idle ground pickup — items are now split atomically instead of copied
- Fixed getTargets() returning raw mutable cache references — workers now get defensive copies, preventing concurrent modification
- Cached PokemonProfile job eligibility list instead of re-concatenating every call
- Block breaking in harvesters now fires proper block-break events for mod compatibility (breakBlock instead of setBlockState AIR)
- Replaced Math.random() with world.random in all combo jobs for deterministic server-side RNG
- WorkSpeedBoostManager methods are now side-effect-free (expiry pruning extracted to shared helper)
- Battle handler errors are now logged instead of silently swallowed

## 3.0.30
- Reduced idle downtime — Pokémon no longer go lazy for long stretches between jobs
- Scan cooldown reduced from 60s → 20s (configurable via `scanCooldown`)
- Target grace period reduced from 15s → 10s (configurable via `targetGrace`)
- Escalating blacklist softened: 10s / 30s / 2min (was 15s / 60s / 5min), all configurable
- New config commands: `/cobblecrew config scanCooldown|targetGrace|blacklistShort|blacklistMedium|blacklistLong <value>`

## 3.0.29
- Fixed Pokémon getting stuck staring at logs/blocks that were already harvested — stale cache entries are now removed immediately when the target is found to be air
- Air blocks are also filtered out during job availability checks so Pokémon don't even attempt stale targets
- Tree Feller combo (cut + headbutt) now breaks the ENTIRE tree at once (up to 64 connected logs) instead of one block at a time

## 3.0.28
- Fixed harvesters (loggers, miners, crop pickers, etc.) failing to actually harvest — claim timer expired before Pokémon reached the target, causing escalating blacklists that locked out all nearby blocks
- Claims are now renewed each tick while the Pokémon is actively navigating to or working on a target

## 3.0.27
- Removed the species_drops producer entirely — Cobblemon's native pasture drop system handles species drops now
- All Pokémon now pick up items from the ground when idle (not a formal job — just built-in behavior)
- Items picked up are deposited into nearby containers (or delivered to the player in party mode)

## 3.0.26
- Removed all 25 hardcoded producer jobs (wool producer, egg layer, milk producer, etc.)
- Replaced with a single "species_drops" producer that uses each Pokémon's actual Cobblemon drop table
- Any Pokémon with drops defined in its species/form data now produces those items on a 120s cooldown
- No more move/species gating — if your Pokémon has drops, it works
- Loot-based workers (fishing, pickup, dive, dig site) are unchanged
- Combo production jobs (string crafter, book binder, etc.) are unchanged

## 3.0.25
- Support Pokémon now provide a work speed aura — nearby workers in the same pasture (or party) get reduced production cooldowns
- Speed Booster and Haste Provider give 15% faster cooldowns, Healer/Strength/Resistance/Hunger give 5%, Aura Master combo gives 20%, Full Restore combo gives 10%
- Boosts stack diminishingly (multiplicative) with a 40% max reduction cap
- Affects all production jobs, environmental jobs (cauldron fillers, fuelers, pollinator), and the scout

## 3.0.24
- Added /cobblecrew party toggle — any player can disable party jobs for themselves so their sent-out Pokémon stop doing work
- Party status now shows your personal opt-out state

## 3.0.23
- Tree felling now breaks the farthest connected log (by BFS depth) instead of the highest — prevents disconnecting arches and branches that would leave floating pieces

## 3.0.22
- Tree-cutting jobs now fell trees top-down — Pokémon scans connected logs and breaks the highest one first, preventing floating tree canopies
- Applies to overworld logger, jungle chopper, fungi harvester, and tree feller combo

## 3.0.21
- Disabled Pokémon cries during job animations — workers still play visual animations but no longer make sounds

## 3.0.20
- Tree planter now enforces 4-block minimum spacing between saplings and avoids planting near existing trees so saplings actually grow

## 3.0.19
- Fixed idle Pokémon playing sleep animation while walking back to the player/pasture
- Idle animations now only play when the Pokémon has actually reached its origin
- Replaced "sleep" with "yawn" in idle and cooldown animation fallback chains

## 3.0.18
- Fixed party workers unable to find targets — Cobblemon entities don't support vanilla pathfinding queries when sent out by the player, so party workers now skip pathfind validation and use direct distance-based target selection instead
- Added party scan logging to show what blocks the eager scanner finds

## 3.0.17
- Workers now consider ALL known moves (active + benched) for job eligibility, not just the 4 equipped moves

## 3.0.16
- Fixed harvesters endlessly re-assigning jobs when targets exist in cache but are unreachable via pathfinding

## 3.0.15
- Increased party and pasture Pokémon movement speed from 0.35 to 0.5 for faster job completion

## 3.0.14
- Fixed harvesters not clearing cache after breaking blocks, causing "target invalid: block is air" spam and massive re-targeting loops
- Harvested blocks now immediately removed from cache to prevent stale target evaluation

## 3.0.13
- Major server performance fix — pasture Pokémon dispatch now runs every 5 ticks instead of every tick, staggered per-pasture to spread load
- Fixed support jobs (healer, resistance, speed, etc.) causing a tight loop — now cooldown for 10+ seconds after applying an effect instead of immediately re-evaluating
- Fixed resetAssignment() needlessly destroying the Pokémon profile cache, forcing a full 128-worker re-evaluation on every reset
- Increased default blocksScannedPerTick from 15 to 50 (max raised to 100) — block scans complete in ~3 seconds instead of ~11
- Party scanner no longer thrashes the cache when a player moves 1 block — requires 3+ blocks of movement before rescanning

## 3.0.12
- Fixed animation packets never reaching clients — the exclusion filter was inverted, silently dropping all packets
- Reduced animation send range from 256 to 128 blocks (matches Cobblemon's default)

## 3.0.11
- Fixed animation fallback chains not actually working — only the first animation was ever tried
- Now sends the full fallback set to the client, which picks the first animation that exists on the Pokémon's model
- Pokémon like Dugtrio and Sandslash (which lack "physical") now correctly fall back to "cry" or other available animations

## 3.0.10
- Pokémon now play species-specific animations during work (physical, special, happy, angry, etc.) instead of a generic hand-swing
- Each job phase has its own animation fallback chain — if a Pokémon doesn't have the preferred animation, it tries the next in the list
- Idle Pokémon at their origin now play ground_idle; after 30 seconds of boredom they switch to unamused/sleep
- Defenders play an angry reaction when spotting a hostile
- Deposit arrival, success, and work-complete moments each trigger distinct reactions
- Debuff-style defense jobs (Repeller, Fearmonger, Sentry, Poison/Ice Trap) use special/status animations instead of physical

## 3.0.9
- Replaced deferred block scanner for party workers with eager synchronous scan
- Party Pokémon now find targets in ~2 seconds instead of ~16 seconds
- Scan runs every 40 ticks (2s) with a smaller radius (6 blocks, height 3)
- Removed zone transition / pinnedOrigin complexity — scan always follows the player
- Added party-specific config: scanIntervalTicks, searchRadius, searchHeight

## 3.0.8
- Fixed party Pokémon scans never completing — zone transition was clearing in-progress scans every tick
- Party workers (sand_miner, etc.) now correctly find nearby blocks and start working
- Throttled "no eligible jobs" debug log to once per 30s instead of every tick

## 3.0.7
- Fixed stale job eligibility when Pokémon moves change (teaching/forgetting moves while in pasture now immediately updates eligible jobs)
- Previously, cached profiles weren't invalidated when moves changed — Pokémon could keep doing jobs they no longer qualified for

## 3.0.6
- Fixed workers unable to find containers during scan rebuilds (cache was wiped before scan started, leaving 0 containers for entire scan duration)
- Scanner now uses double-buffering: scans into staging area, swaps atomically on completion — old cache stays available throughout
- Rate-limited deposit failure warnings to once per 10 seconds per Pokémon (was spamming every tick)

## 3.0.5
- Added diagnostic logging when workers can't find containers to deposit items
- Logs: cached container count, tried/failed count, scan status, and which items couldn't be deposited
- Logs when specific containers are rejected (no space or missing block entity)

## 3.0.4
- Workers now hold items and retry containers instead of dropping items on the ground when all containers are full
- Retry cycle: when all nearby containers are exhausted, waits 10 seconds then re-checks (containers may have been emptied)
- Increased overflow safety timeout from 30 seconds to 5 minutes before force-dropping as last resort

## 3.0.3
- Eliminated ALL block/item tag usage in favor of direct block class and hardcoded checks
- Fixes apricorn, mint, flower, leaf, fire, sapling, and raw material detection that could silently fail due to tag resolution issues
- Cobblemon blocks now use instanceof checks (ApricornBlock, BerryBlock, MintBlock)
- Vanilla blocks use hardcoded sets or class checks (LeavesBlock, SaplingBlock, etc.)

## 3.0.2
- Added diagnostic logging for job assignment and block scan results
- Fixed species name resolution (use species.name instead of translatedName for reliable matching)

## 3.0.1
- Fixed Sand Miner job never finding sand blocks (used nonexistent minecraft:sand block tag; now uses direct block check like other earth categories)

## 3.0.0
- Full rebrand: Cobbleworkers → CobbleCrew
- New mod ID: cobblecrew (was cobbleworkers)
- New package: akkiruk.cobblecrew (was accieo.cobbleworkers)
- Config migration: existing cobbleworkers configs are auto-copied to cobblecrew on first launch
- Command changed from /cobbleworkers to /cobblecrew

## 2.12.2
- Pasture Pokémon no longer trample farmland when walking over crops

## 2.12.1
- Fixed berry harvester not detecting all berry types (now uses direct BerryBlock type check instead of relying solely on tag)
- Berry harvester no longer double-resets block state after Cobblemon's harvest() already handles it
- Added pre-harvest validation so Pokémon skip berries that were already picked between targeting and arrival
- All harvesters now release stale targets when the block is no longer harvestable (not just when broken)

## 2.12.0
- ~70% of jobs now require exactly 1 specific move for clear specialization
- ~30% of jobs keep 2 moves, only where both are a genuinely perfect thematic fit
- Removed overly common moves (bite, ember, watergun) and legendary signatures (glaciate, diamondstorm, magmastorm, sacredsword)
- Each job's move was chosen for thematic resonance and moderate rarity

## 2.11.1
- Added species-based eligibility for thematically fitting Pokémon (e.g. Magnemite→Magnetizer, Combee→Bee Pollinator/Honey/Wax, Chansey→Healer/Egg Layer, Miltank→Milk, Wooloo→Wool, Meowth→Coin Minter, Delibird→Gift Giver, Tropius→Berry/Fruit, etc.)
- These Pokémon can always do their thematic job regardless of current moveset
- Server owners can customize species lists per job via JSON config

## 2.11.0
- Job eligibility is now purely move-based: Pokémon must know 1-2 specific moves to qualify for each job
- Removed type and species fallbacks that made every Pokémon of a given type eligible for dozens of jobs
- Trimmed qualifying moves per job to 1-2 (down from 3-4) for clearer specialization
- Server owners can still override qualifying moves per job via JSON config

## 2.10.7
- Removed trash_disposal job (item deletion risk)

## 2.10.6
- Pokémon now check if a chest has space before interacting with it
- Full chests are skipped silently instead of being opened/closed pointlessly
- Container search filters out full chests so Pokémon don't even walk to them

## 2.10.5
- Pokémon now shuffle through all eligible jobs and pick the first one with actual work available, instead of getting stuck on unavailable jobs
- Removed priority tier lockout: Pokémon can now do ANY eligible job (combo/move/species/type) instead of being locked to only the highest tier
- Support workers (Speed, Strength, Haste, etc.) now buff full-health players; only Healer requires damaged targets
- Bonemeal Applicator now skips fully grown crops instead of wasting bone meal on them
- Sentry now applies Glowing + Weakness to hostiles instead of just spawning particles
- Added isAvailable checks to BaseHarvester, BaseDefender, BaseSupport, and all environmental jobs so the dispatcher can properly skip jobs with no targets
- Pokémon return to pasture when no jobs have available work instead of wandering aimlessly

## 2.10.4
- Fixed Paper Maker and Composter silently destroying items (extracted 1 item but recipes need 3 and 7)
- Fixed Scout resetting its 10-minute cooldown before finding a map, blocking all subsequent attempts
- Fixed Healer/support workers calling wrong release function when no players nearby (leaked player claims)
- Added cleanup override to BaseSupport so player target claims are released when Pokémon leave pasture

## 2.10.3
- Workers now navigate immediately when claiming a target (eliminates 1-tick hesitation)
- Pathfinding recalculation interval reduced from 20 to 5 ticks for smoother movement
- Fixed harvest targeting wrong block (closestTarget vs claimed currentTarget)
- Fixed workers not navigating at all when closest target differs from claimed target
- Added air-block validation: workers abandon targets destroyed by other players
- Work timer grace period: bumping a Pokémon briefly no longer resets its 1.5s work animation
- Increased default work range from 1 to 3 blocks so shorter Pokémon can reach apricorns, crops, etc.
- Job stickiness: workers now keep the same job for at least 5 seconds before re-rolling
- Claim timeout reduced from 7s to 5s for faster recovery from stuck pathfinding
- Chest/barrel open/close animation when depositing items with a visible work delay
- Applied all navigation fixes consistently across all 25+ jobs (environmental, logistics, production, support)

## 2.10.2
- FrostFormer, ObsidianForge, and GrowthAccelerator now use the deferred block cache instead of scanning ~4,913 blocks every tick
- Added WATER, LAVA, and GROWABLE block category validators for the deferred scanner
- BaseDefender hostile-mob scan throttled to every 20 ticks (was every tick)
- Deferred scanner now only runs validators for categories used by registered jobs (skips ~30 unused validators per block)

## 2.10.1
- Removed server-side TM lore enrichment (moved to CobbleDex client-side tooltip instead)
- Removed getMoveToJobsIndex() and MoveJobEntry from CobbleworkersApi

## 2.10.0
- Added job descriptions to all 80+ jobs in CobbleworkersApi (fixes empty descriptions in CobbleDex)
- Fixed field name mismatch: API now sends hardcodedSpecies/hardcodedSpeciesEnabled (matching CobbleDex wire format)
- Fixed requiredAbility not being sent in job rules API
- Added TM lore enrichment: TMs in player inventories now show which Cobbleworkers job(s) the move enables
- TM lore is server-side only — no client mods needed, works with vanilla client
- Combo jobs show required companion moves on TM tooltips
- Added CobbleworkersApi.getMoveToJobsIndex() for reverse move→jobs lookup

## 2.9.0
- Replaced all 22 legacy hardcoded jobs with the V2 DSL/Worker system
- Legacy jobs are now split into more specific sub-jobs (e.g. HoneyCollector → honey_harvester + bee_pollinator)
- Removed CobbleworkersConfigHolder group classes for all individual jobs; general settings (searchRadius, searchHeight, blocksScannedPerTick) retained
- All job eligibility (types, species, moves, cooldowns, radii, etc.) now configured via per-category JSON under config/cobbleworkers/
- Removed JobType enum, CobbleworkersConfigPokemonType enum, CobbleworkersTypeUtils, TypeExtensions, and all 22 legacy job files
- Removed legacyRules() from CobbleworkersApi; API now returns only V2 DSL jobs

## 2.8.0
- Added S2C network packet that sends job rules to connected clients on join
- Enables client-only mods (like CobbleDex) to receive job data without needing server-side installation
- Clients without a handler silently ignore the packet (no crash)

## 2.7.1
- Fixed init order: WorkerRegistry.init() now runs before JobConfigManager.load()
- DSL jobs register their default configs during init, so load() must happen after
- This was causing JobConfigManager to have 0 entries, making the API return empty data for all DSL jobs

## 2.7.0
- CobbleworkersApi.getJobRules() now returns all DSL jobs (gathering, production, processing, placement, defense, support, environmental, logistics, combo)
- Previously only returned the 22 legacy jobs, causing CobbleDex to miss ~80 new jobs
- Added /cobbleworkers debug command (op-only) — generates full diagnostic report and uploads to mclo.gs
- Added CobbleworkersApi.generateDiagnosticReport() for programmatic diagnostics

## 2.6.0
- Added public CobbleworkersApi for direct mod integration (replaces file/reflection approach)
- Removed CobbledexDataProvider and file-based job rule export

## 2.5.0
- Added CobbleDex integration: exports all 22 job eligibility rules via reflection API
  so CobbleDex can display which Pokémon qualify for which Cobbleworkers jobs

## 2.4.0
- Phase 8: 3 environmental jobs (frost former, obsidian forge, growth accelerator)
- Phase 8: 1 logistics job (magnetizer)
- Phase 9: 18 combo jobs requiring multiple moves for enhanced effects
  - 7 gathering combos (demolisher, fortune miner, silk touch extractor, vein miner,
    tree feller, fossil hunter, gem vein finder)
  - 8 production combos (string crafter, book binder, candle maker, chain forger,
    lantern builder, banner creator, deep sea trawler, magma diver)
  - 1 processing combo (blast furnace with 2x output)
  - 2 support combos (full restore, aura master)

## 2.3.0
- Phase 3: 25 production jobs (wool, silk, slime, ink, bone, pearl, feather, scale, fruit,
  coin, gem, spore, pollen, gift, egg, milk, electric, wax, powder, ash, static, sap, toxin,
  crystal, tear)
- Phase 4: 10 processing jobs (ore smelter, food cooker, glass maker, brick baker, charcoal
  burner, paper maker, bone grinder, flint knapper, pigment presser, composter)
- Phase 5: 4 placement jobs (torch lighter, tree planter, crop sower, bonemeal applicator)
- Phase 6: 7 defense jobs (guard, sentry, repeller, fearmonger, fire/poison/ice trap)
- Phase 7: 9 support jobs (healer, speed/strength/resistance/haste/jump booster, night vision,
  water breathing, hunger restorer)

## 2.2.0
- Phase 2: 28 new gathering jobs via DSL (wood, crops, stone, minerals, nature)
- BlockCategoryValidators: 30+ new validators for all gathering block types
- Jobs: Overworld Logger, Jungle Chopper, Fungi Harvester, Bamboo Chopper, Sugar Cane Cutter,
  Cactus Pruner, Vine Trimmer, Beet Puller, Sweet Berry Harvester, Pumpkin/Melon Harvester,
  Cocoa Harvester, Chorus Fruit Harvester, Glowberry Picker, Dripleaf Harvester, Stone Breaker,
  Igneous Miner, Deepslate Excavator, Excavator, Sand Miner, Clay Digger, Sculk Harvester,
  Ice Miner, Mushroom Forager, Flower Picker, Snow Scraper, Moss Scraper, Decomposer, Terrain Flattener

## 2.1.0
- Phase 1: Base class infrastructure for V2 job system
- PokemonProfile: eligibility caching computed once per Pokémon on pasture entry
- WorkerDispatcher: profile-based job assignment with stickiness + isAvailable filtering
- JobConfig + JobConfigManager: per-job JSON config under config/cobbleworkers/
- New base classes: BaseProducer, BaseSupport, BaseProcessor, BasePlacer, BaseDefender
- DSL builders: GatheringJob, ProductionJob, ProcessingJob, PlacementJob, DefenseJob, SupportJob
- Registry categories: LegacyJobs + 9 category stubs for future DSL-based jobs

## 2.0.0
- V2 Architecture: BlockCategory-based scanner, WorkerPriority tiers, WorkerRegistry
- All 22 jobs declare V2 metadata (name, targetCategory, isEligible)
- DeferredBlockScanner: fixed config caching bug, added chunk-loaded guard, dual category+legacy scanning
- BaseHarvester: container overflow timeout (30s), harvestTool parameter
- CobbleworkersCacheManager: BlockCategory API, removeTargetGlobal for overlapping pastures
- CobbleworkersNavigationUtils: mob targeting for future combat jobs
- CobbleworkersInventoryUtils: barrel extraction methods (findInputContainer, extractFromContainer)
- WorkerDispatcher: now uses WorkerRegistry instead of hardcoded list

## 1.9.0
- Added visual feedback: Pokémon swing hands, look at targets, spawn particles, and cry when working
- Added one-job-at-a-time system: Pokémon focus on completing one task before switching jobs
- Added EXCITED behavior flag while working and return-to-pasture when idle
- Refactored all harvesters to use BaseHarvester class (eliminated 161 lines of duplicate code)
- All workers now have 1.5-second work delays with animations at targets

## 1.8.0
- Fixed an issue with combee evolution line not generating honey if game was in a different language.
- By default, irrigators now irrigate a 3x3 area (9 blocks). This setting is configurable.
- By default, fire extinguishers now extinguish a 3x3 area (9 blocks). This setting is configurable.
- By default, Ditto will now handle all jobs instead of Pikachu. (Old configs will keep Pikachu)
- Vivichoke is now replanted.
- Added support for hearty grains and galarica nut bush.